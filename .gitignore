# .NET
src/**/bin/
src/**/obj/
tests/**/bin/
tests/**/obj/
*.user

# Angular / Node
client/node_modules/
client/dist/
client/.angular/
npm-debug.log*

# Env / secrets
.env
.env.*
!.env.example
# Local test logins (seeded owner/employee accounts) — never belongs in the repo,
# and it sat untracked-but-uncommitted long enough to be committed by accident.
credentials.txt

# Docker / data volumes (named volumes, not repo paths — kept defensive only)
**/pgdata/

# Deploy artifacts (generated by deploy/deploy.sh, deploy/backup.sh — draft/unverified, E2-07/E2-08)
efbundle
efbundle.pdb
/backups/

# OS / editor
.DS_Store
Thumbs.db
.vs/
.vscode/
*.swp

# Human task register (guardrail G2) - updated every round, so it would otherwise
# churn git status continuously. Committed once at 4620947 as a snapshot.
# NOTE: the file is currently still TRACKED, so this entry is inert - .gitignore
# only affects untracked files. Making this effective needs
#   git rm --cached docs/HUMAN_TASKS.md
# which un-commits it from the tree while leaving it on disk. Deliberately NOT
# done yet - awaiting an explicit call, see ACTION_PLAN section 18.
docs/HUMAN_TASKS.md
